#include <bits/stdc++.h>
using namespace std;

int main(int argc, char** argv) {
//	pair<int, int>v = make_pair(100,200);
//	cout<<v.first<<endl;
//	cout<<v.second<<endl;
//	tuple<int,int,int>v=make_tuple(100,200,300);
//	cout<<get<1>(v)<<endl;
//	cout<<get<2>(v)<<endl;
	string s;
	getline(cin, s);
	int dem=0;
	for(int i=0; i<s.size(); i++){
		if(s[i] == 'a'){
			dem++;
		}
//		pair<string, int> v = (s[i], dem);
//		cout<<v.first<<" ";
//		cout<<v.second<<" ";
		map<int,int>mp;
		mp(dem)
	}
//	pair<string, int> v = make_pair("a", dem);
//		cout<<v.first<<" ";
//		cout<<v.second<<" ";
	return 0;
}
